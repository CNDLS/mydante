%h1 Welcome to Proust!
%h3
  Please sign in through one of theses services:
-#:ldap, :twitter, :google_apps, :open_id, :facebook, :shibboleth
- [:ldap].each do |provider|
  - provider_label = provider.to_s.titleize

  - case provider
  - when :google_apps
  - when :open_id
    - provider_label = "OpenID"
  - when :ldap
    - provider_label = "Georgetown"
      
  %a.auth_provider{ :href => user_omniauth_authorize_path(provider) }  
    =image_tag("#{provider}_64.png", :size => "64x64", :alt => "#{provider_label}")
    =provider_label